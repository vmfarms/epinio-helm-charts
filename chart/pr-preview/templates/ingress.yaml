apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-el-ingress
  annotations:
    external-dns.alpha.kubernetes.io/target: lb-01.shared-clients-001.mtl02.stack.io
    acme.cert-manager.io/http01-edit-in-place: "true"
    cert-manager.io/cluster-issuer: nginx-letsencrypt-prod
  labels:
    {{- range $key, $value := .Values.labels }}
    {{ $key }}: {{ $value | quote }} 
    {{- end }}
    {{- include "pr-preview.labels" . | nindent 4 }}
spec:
  ingressClassName: nginx
  rules:
  - host: {{ .Release.Name }}.epinio.demo.stack.io
    http:
      paths:
      - backend:
          service:
            name: el-{{ .Release.Name }}-pr-el
            port:
              number: 8080
        path: /
        pathType: ImplementationSpecific
  tls:
  - hosts:
    - {{ .Release.Name }}.epinio.demo.stack.io
    secretName: {{ .Release.Name }}.epinio.demo.stack.io